# 💻 TypeScript: ইন্টারফেস (Interfaces) এবং টাইপ (Types) এর মধ্যে পার্থক্য

TypeScript-এ ডেটার গঠন সংজ্ঞায়িত করার জন্য `interface` এবং `type` উভয়ই গুরুত্বপূর্ণ ভূমিকা পালন করে। নিচে এদের প্রধান পার্থক্যগুলি তুলে ধরা হলো।

---

## ১. এক্সটেনশন এবং মার্জিং (Extension and Merging)

| বৈশিষ্ট্য | ইন্টারফেস (`interface`) | টাইপ (`type`) |
| :--- | :--- | :--- |
| **এক্সটেনশন পদ্ধতি** | একে অপরের বৈশিষ্ট্য **`extends`** কী-ওয়ার্ড ব্যবহার করে উত্তরাধিকার সূত্রে পেতে পারে। | **`&` (Intersection)** অপারেটর ব্যবহার করে একাধিক টাইপের বৈশিষ্ট্য একত্রিত করতে পারে। |
| **ডিক্লেয়ারেশন মার্জিং** | একই নামের একাধিক ইন্টারফেস স্বয়ংক্রিয়ভাবে মার্জ হয়ে যায়। এটি স্কেলেবল লাইব্রেরি তৈরির জন্য খুবই উপযোগী। | একই নামের দুটি টাইপ তৈরি করা সম্ভব নয়; এটি ত্রুটি (Error) দেবে। |
| **উদাহরণ (এক্সটেনশন)** | `interface Person extends NamedEntity {}` | `type FullPerson = NamedEntity & Address;` |

## ২. টাইপস অফ ডেটা (Applicable Data Types)

`type` `interface`-এর তুলনায় অনেক বেশি বহুমুখী ডেটা সংজ্ঞায়িত করতে পারে।

### ✅ ইন্টারফেস দিয়ে যা করা যায়:

* অবজেক্টের গঠন (Object Shapes)
* ফাংশন সিগনেচার (Function Signatures)
* ক্লাসের চুক্তি (Class Contracts - `implements` ব্যবহার করে)

### ✅ টাইপ দিয়ে যা করা যায়:

* **উপরে উল্লিখিত সবগুলি**
* **ইউনিয়ন (Union) টাইপস:** একাধিক সম্ভাব্য টাইপ সংজ্ঞায়িত করে।
    > ```typescript
    > type ID = number | string;
    > ```
* **প্রিমিটিভস এবং লিটারেলস:** সরাসরি প্রিমিটিভ (যেমন: `string`) বা নির্দিষ্ট লিটারেল মান সংজ্ঞায়িত করে।
    > ```typescript
    > type Status = "success" | "error" | "pending";
    > ```
* **টিউপলস (Tuples):** একটি নির্দিষ্ট সংখ্যক উপাদান সহ একটি অ্যারে।
    > ```typescript
    > type Coords = [number, number];
    > ```
* **কন্ডিশনাল এবং ম্যাপড টাইপস:** জটিল লজিক্যাল টাইপ ম্যানিপুলেশনের জন্য অপরিহার্য।

## ৩. ব্যবহারের সাধারণ ক্ষেত্র (When to use which?)

### 🚀 ইন্টারফেস কখন ব্যবহার করবেন?

* যখন আপনি **অবজেক্টের গঠন** বা **ক্লাসের চুক্তি** সংজ্ঞায়িত করছেন।
* যখন আপনার প্রয়োজন **ডিক্লেয়ারেশন মার্জিং**-এর সুবিধা (যেমন: লাইব্রেরি বা থার্ড-পার্টি মডিউলগুলিতে নতুন বৈশিষ্ট্য যোগ করা)।

### ✨ টাইপ কখন ব্যবহার করবেন?

* যখন আপনি **ইউনিয়ন বা ইন্টারসেকশন** ব্যবহার করে জটিল টাইপ তৈরি করছেন।
* যখন আপনি কোনো **প্রিমিটিভ** বা **লিটারেল** টাইপের আলিয়াস (Alias) তৈরি করতে চান।
* যখন আপনি **কন্ডিশনাল বা ম্যাপড টাইপস** ব্যবহার করে উন্নত টাইপ লজিক তৈরি করতে চান।